# Copy to .env in this folder:  copy .env.example .env
# Gemini is required for embeddings (batch) and chat (query). Add when ready:
GEMINI_API_KEY=

# Optional: override JWT signing secret for auth-service (default in compose is dev-only)
# JWT_SECRET=
