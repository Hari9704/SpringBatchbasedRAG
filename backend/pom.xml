<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.0</version>
        <relativePath/>
    </parent>

    <groupId>com.docintell</groupId>
    <artifactId>docintell-ai-platform</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>DocIntell AI Platform</name>
    <description>AI-Driven Document Intelligence Platform</description>

    <modules>
        <module>api-gateway</module>
        <module>auth-service</module>
        <module>document-service</module>
        <module>batch-service</module>
        <module>query-service</module>
        <module>feedback-service</module>
        <module>analytics-service</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <spring-cloud.version>2025.0.0</spring-cloud.version>
        <!-- Explicit version overrides to address remaining CVEs not yet covered by the Spring Boot 3.5.0 BOM -->
        <tomcat.version>10.1.41</tomcat.version>
        <spring-security.version>6.5.0</spring-security.version>
        <spring-framework.version>6.2.8</spring-framework.version>
        <netty.version>4.1.121.Final</netty.version>
        <reactor-netty.version>1.2.6</reactor-netty.version>
        <logback.version>1.5.19</logback.version>
        <jackson-bom.version>2.19.0</jackson-bom.version>
        <bouncycastle.version>1.80</bouncycastle.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>1.0.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Override BouncyCastle to address GHSA-c3fc-8qff-9hwx -->
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
