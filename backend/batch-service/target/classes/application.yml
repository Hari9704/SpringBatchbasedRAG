server:
  port: 8083
spring:
  application:
    name: batch-service
  datasource:
    url: jdbc:h2:mem:batchdb
    driver-class-name: org.h2.Driver
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false
